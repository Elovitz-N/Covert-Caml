(** client.ml contains the client application code that gets executed
    when the binary is run. Functions in client.ml should not be called
    from outside of client.ml and are therefore not exposed. *)
